# CloudComputingA2
Hello team! Keep this updated with versions we have pushed to main. It will be date format eg. DD.MM.YY
Make sure to communicate to team BEFORE pushing to main!!!
Thank you!

When you use or adapt code developed by someone else, you must include an inline comment in the code citing your source.

Using Elastic Beanstalk is NOT allowed as a valid means of deploying your application.

Ensure your web application runs on the standard HTTP(S) ports: 80 or 443.

Students would also need to securely access the objects stored in S3. This is considered security best practice and should always be done.

Students must carefully design the DynamoDB key schema and indexing strategy, including at least one Global Secondary Index (GSI) and one Local Secondary Index (LSI), to support efficient query patterns. Indexes should be purposefully designed rather than added arbitrarily.

Both the Query and Scan operations should be implemented appropriately for data retrieval from DynamoDB tables.

Please ensure that your implementation correctly handles different HTTP request methods (including GET, POST, and DELETE) when using the API Gateway REST API, and that each method is properly mapped to its corresponding database operation. Students are expected to implement a genuinely RESTful API, rather than routing all actions through a generic POST request.

V21.04.2026